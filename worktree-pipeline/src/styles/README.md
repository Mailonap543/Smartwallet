# src/styles

Diretório reservado para estilos globais estáticos do backend (por exemplo, assets servidos via Spring static resources). No momento, o projeto usa os estilos globais do frontend em `smartwallet-front/src/styles.css`; nenhum CSS é necessário aqui. Mantido para compatibilidade com pipelines e futuras necessidades de assets.
