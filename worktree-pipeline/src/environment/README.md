# src/environment

Diretório reservado para arquivos de configuração de ambiente (ex.: `.env`, perfis externos ou templates de variáveis). No momento não há arquivos necessários aqui; mantenha para futuros pipelines ou para armazenar exemplos de configuração que não devam ir em `resources`.
